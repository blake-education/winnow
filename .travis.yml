language: ruby
rvm:
  - 2.5.1
before_install:
  - gem update --system
  - gem install bundler -v "~> 1.17.3"
before_script:
  cd spec/dummy && bundle exec rake db:test:load && cd ../..
gemfile:
  - Gemfile
notifications:
  email:
    - dev@blake.com.au
